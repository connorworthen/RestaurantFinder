const list = document.querySelector(".list");

const sort_name_btn = document.querySelector(".sort-options .sort-name");
const sort_name_btn = document.querySelector(".sort-options .sort-meta");
const sort_name_btn = document.querySelector(".sort-options .sort-age");
